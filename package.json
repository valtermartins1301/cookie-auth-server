{
  "name": "cookie-auth-server",
  "version": "1.0.0",
  "description": "nodejs server for authenticate and use set-cookie on response",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/cookie": "^11.0.0",
    "@hapi/hapi": "^19.1.1",
    "bcrypt": "^4.0.1"
  }
}
